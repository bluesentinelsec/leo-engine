Concept:

```
leo-engine new <dir>                 # bootstrap project
leo-engine run [--debug …]           # run game
leo-engine watch                     # run with hot reload
leo-engine pack assets/ game.leopack # resource pack
leo-engine build --platform win64    # produce distributables
leo-engine profile --record          # capture frame timings
```
