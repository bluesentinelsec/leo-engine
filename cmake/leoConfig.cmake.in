@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find SDL3 dependency
if(@LEO_VENDOR_SDL@)
    # SDL3 is bundled with Leo
    find_dependency(SDL3 REQUIRED)
else()
    # Consumer must provide SDL3
    find_dependency(SDL3 REQUIRED)
endif()

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/leoTargets.cmake")

check_required_components(leo)
